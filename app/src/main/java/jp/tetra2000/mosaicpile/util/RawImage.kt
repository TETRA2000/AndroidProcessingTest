package jp.tetra2000.mosaicpile.util

class RawImage(data: ByteArray, format: Int, width: Int, height: Int) {
    val data = data
    val format = format
    val width = width
    val height = height
}